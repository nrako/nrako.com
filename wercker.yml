box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: build static sources
        code: |
          echo "wintersmith build"

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GITHUB_API_TOKEN
        domain: www.nrako.com
        basedir: build
        repo: nrako/nrako.github.io
