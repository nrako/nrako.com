import { assertEquals } from '@std/assert'

Deno.test('index', () => {
  // Temporary test placeholder.
  // TODO This should be removed once it get clearer what's the approach to test
  // route and handler in Fresh v2
  assertEquals(2, 2)
})
