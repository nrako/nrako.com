div#flashmsgs
  if hasMessages
    - var objMsg = messages();
    each msgs, type in objMsg
      each msg in msgs
        div(class='alert alert-' + type)!= msg
          a.close(data-dismiss="alert", href="#") &times;